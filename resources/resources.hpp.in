#pragma once

#include <array>
#include <string>

struct Day {
    char const * name;
    std::string (*function)(char const * input);
    char const * default_input_file_path;
};

@DAYS_FUNCTIONS@

@DAYS_DATA@
